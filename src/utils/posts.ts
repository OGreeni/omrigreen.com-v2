// TODO: move getUniqueTags logic here

// export const getUniqueTags = async () => {
//   const posts = await import.meta.glob('./posts/*.md');
//   const uniqueTags = [
//     ...new Set(posts.map((post) => post.frontmatter.tags).flat()),
//   ];
// };

export {};
